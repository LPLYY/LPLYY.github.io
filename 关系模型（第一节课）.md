#### 关系模型是当下大多数数据库采用的方案。####
Oracle靠这个赚的盆满钵满。

## 概述
Andy 在第一堂课上关于**数据库**的优越性讲解了许多，笔者简单概括一下。

	1查找速度快  2 数据安全高  3 可移植

同时SQL也具有其优点，对于我们的学习并无影响，笔者建议感兴趣的读者自行观看，在笔者提供的视频链接第一节课。（不是笔者翻译的，感谢汉化组）

下面笔者介绍一些有关**关系模型**的东西。

数据库并非全部是关系模型，但可以肯定地说，关系模型目前是大多数数据库的选择，cmu15445也是围绕关系模型数据库展开的。

笔者认为关系模型数据库最具革命性的在于**数据独立性** ，我们不再关心数据的物理存储结构，数据物理存储和逻辑上的使用数据完全可以分开，当然这也是SQL的优势。我们可以这样想象一下，当你使用mysql，pg，或者duckdb这种轻量数据库，从用户角度看我们从不关心数据究竟在磁盘里长什么样子，但是历史方案在这点上做的很差，程序很大程度上依赖数据的结构，程序员需要根据数据的结构硬编码对应的处理方案，这会导致很多问题，比如数据改变了，程序需要维护，等等。当然，有一点点跑题，数据的独立性也在于数据通过关系在一起，而并非树状或者网状，这意味着我们调用数据不必关心他的上一层级，这对程序员和用户都是很好的，所以关系模型胜出了

关系模型中，我们会用到**主键**，用做于在关系中唯一标识单个元组的方法，我们也可以定义其他列作为唯一值（该列元素不可重复，比如身份证号这类），但并不是总被看作为标识列。通常，为数据引入一个**编号**是个不错的选择。
例如：

| name | year | country |     |
| :--- | :--: | ------: | --- |
| Tom  |  18  |     USA |     |
| Jack |  19  |     USA |     |

| id  | name | year | country |
| --- | :--- | :--: | ------: |
| 101 | Tom  |  18  |     USA |
| 102 | Jack |  19  |     USA |
在许多框架中，这种编号是可以自动生成的，当你插入多一条数据时，编号会自动+1，或者按照你想要的规则改变，还不错，是吧。


关联不同的表是其重要应用之一，比如我们假设上面是两个音乐人的信息，下面是两张专辑的名字。

| id  | name   |
| --- | :----- | 
| 11  | gogogo |
| 12  | nonono |  
我们可以通过id来关联两张表，尤其是处理一张专辑有多个创作者的时候，比如

| artist_id | album_id |     |
| --------- | :------- | --- |
| 101       | 11       |     |
| 102       | 11       |     |
| 102       | 12       |     |
我们也可以通过**约束**来限制我们插入的数据，比如我们可以通过一些方式限制album_id不为空，具体内容将在SQL章节展开。

一般来说含有 _ 的列名即为主键，所以我们可以看到，一个表可以由多个列来构成主键 。

## 关系代数
原课在这一部分同时介绍了对应的SQL语句，并且默认学生掌握了基础SQL，但是笔者学习的时候并没有很熟练，所以统一放在下一节课，对于基础SQL很熟练的朋友可以跳过下面的内容以及下一章的基础篇。

接下来是**关系代数**，是很重要但并不困难的一部分，掌握这些有助于下一节对于SQL的学习。

笔者这里没有想到比原书更简单贴切的介绍了，推荐读者阅读对应教材 60-67 页，考虑到不清楚版权问题，不能贴出原书，如有需要请留言。

## 结语
这是的第一章内容，笔者认为学习这门课有很多东西是没在教材或者ppt上体现出来的，强烈建议有时间的读者观看Andy的视频课，有一些课堂的问题也带给笔者启发，再次感谢Andy的奉献。
